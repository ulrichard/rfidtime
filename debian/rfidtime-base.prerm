#!/bin/sh

set -e

case "$1" in
	remove|upgrade|remove-in-favour|deconfigure-in-favour)
		#
		# do here pre removes
		#

		# remove the compiled files
		rm -f /usr/bin/rfidtime.pyc
		rm -f /usr/bin/rfidtime.pyo
		;;
	failed-upgrade)
		;;
esac

exit 0
